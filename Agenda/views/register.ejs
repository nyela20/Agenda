<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>S'inscrire</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
</head>
<body>
<main class="flex h-full">
    <div class="w-1/2 flex flex-col items-center justify-center">
        <div class="max-w-sm w-full">
            <div class="w-full">
                <h1 class="text-3xl font-semibold text-primary">S'inscrire</h1>
            </div>
            <form action="/users/register" method="post" class="w-full mt-8 flex flex-col gap-5">
                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="error-message">
                        <%= error %>
                    </div>
                <% } %>

                <div class="space-y-1.5">
                    <label for="name" class="text-secondary">Nom</label>
                    <input class="w-full px-3.5 py-2.5 rounded-lg border border-primary placeholder-primary"
                           type="text" name="name" id="name" placeholder="John Doe" required>
                </div>
                <div class="space-y-1.5">
                    <label for="email" class="text-secondary">Email</label>
                    <input class="w-full px-3.5 py-2.5 rounded-lg border border-primary placeholder-primary"
                           type="email" name="email" id="email" placeholder="Email" required>
                </div>

                <div class="space-y-1.5">
                    <label for="password" class="text-secondary">Mot de passe</label>
                    <input class="w-full px-3.5 py-2.5 rounded-lg border border-primary placeholder-primary"
                           type="password" name="password" id="password" placeholder="••••••••" required>
                </div>

                <div class="space-y-1.5">
                    <label for="confirmPassword" class="text-secondary">Confirmation du mot de passe</label>
                    <input class="w-full px-3.5 py-2.5 rounded-lg border border-primary placeholder-primary"
                           type="password" name="confirmPassword" id="confirmPassword" placeholder="••••••••" required>
                </div>

                <button class="w-full bg-blue-600 hover:bg-blue-700 transition-colors duration-300 ease-in-out text-white px-4 py-2.5 rounded-lg shadow-xs-skeuomorphic border-skeuomorphic font-semibold mt-1"
                        type="submit">
                    S'inscrire
                </button>
            </form>

            <div class="flex items-center justify-center mt-8 gap-1">
                Vous êtes déjà inscrit  ?<a href="/" class="text-blue-600 font-semibold">Connectez-vous
                    ici</a>
            </div>
        </div>
    </div>
    <div class="w-1/2 flex flex-col items-center justify-center px-8 bg-[#F2F4F7] m-6 rounded-lg">
        <h1 class="text-3xl font-medium max-w-xl text-center">
            Organisez votre vie facilement : des agendas sur mesure, créés juste pour vous, avec notre application
            Agenda
        </h1>
    </div>
</main>
</body>
</html>


